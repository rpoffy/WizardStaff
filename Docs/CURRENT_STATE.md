# Current State

**Last Updated:** 2026-08-09
**Purpose:** A current, evidence-based status snapshot. For historical details, use [DESIGN_DECISIONS.md](DESIGN_DECISIONS.md) and [OnlineMultiplayerArchitecturePlan.md](OnlineMultiplayerArchitecturePlan.md).

## Evidence Rules

- **Implemented and verified** means current repository evidence plus a recorded build, direct observation, or human playtest result.
- **Implemented but unverified** means source/configuration exists, but the relevant outcome was not recorded as tested.
- **Partially implemented** means a useful path exists but a boundary, validation, or intended behavior remains incomplete.
- The workspace currently contains substantial uncommitted implementation and documentation work. Treat current source as more current than the `main` branch history.

## Gameplay and Local Play

| Area | Status | Evidence |
| --- | --- | --- |
| Party Hall -> Mug Run -> Staffs at Dawn -> Cauldron Catastrophe -> Grand Wizard Final -> rematch | **Implemented and verified locally and in two-player listen-server PIE** | Multiple standalone playthroughs plus a 2026-07-15 two-player listen-server PIE pass confirmed the loop, intermission handoffs, and gameplay mechanics. |
| Local one-human-plus-bot, local couch, shared camera, keyboard fallback | **Implemented and verified** | Current mode-gating code and preserved-workflow records. Continue to regression-test after changes. |
| Mug pickups, staff growth, Slosh, brew readability | **Implemented and verified** | Current actor/character code and recorded online smoke results. |
| Arcane Pinball | **Implemented and verified** | Server-owned online scaffold, cleanup passes, and reported playthrough success. |
| Quick Bonk, Staff Clash, ring-out/respawn, Mega Staff | **Implemented and verified** | Existing authority/readability code and recorded direct-connect full-loop result. |
| Grand Wizard Final Candidate/steal/winner readability | **Implemented and verified** | Existing server/readability code and recorded full-loop result. |
| Snapped staff segments | **Implemented and verified for authority/count; cosmetic cue unverified** | Local loose physics remains unchanged. Online authoritative loss/count/readability is verified and now reuses the existing snap sequence to create one local, non-colliding segment pop on each machine for 0.85 seconds. The cosmetic has no replicated transform or gameplay effect. `WizardStaffEditor` built successfully on 2026-08-09; host/client cue appearance and cleanup remain to be human-checked. |
| Transition reset/staging responsibility split | **Implemented and verified** | Mug Run state/pickup reset, wizard gameplay reset, arena phase presentation, and wizard placement now have separate explicit helpers. `WizardStaffEditor` built successfully, and a 2026-07-15 two-player listen-server session verified the resulting loop and handoffs. |
| Party Hall fall recovery and camera exclusion | **Implemented and verified** | Current source immediately returns an out-of-bounds intermission wizard to their authoritative Party Hall spawn without Trial score/telemetry, and shared-camera framing ignores a wizard below the hall floor. `WizardStaffEditor` built successfully and the user human-verified the fix on 2026-08-01. |
| Party Hall board dynamic values | **Implemented but unverified** | The replicated Party Hall actor now carries server-published standings, next-Trial/countdown, preset, and leader text snapshots. Clients apply those strings to visual-only text components. `WizardStaffEditor` built successfully on 2026-08-01; a two-player host/client readability check remains required. |
| Frame-independent mouse turning | **Implemented but unverified** | Mouse X now uses a dedicated delta-input path without frame-time multiplication, while keyboard/gamepad turning remains rate-based. Existing Slosh, obstruction, hit-reaction, and Staff Heft modifiers still apply. `WizardStaffEditor` built successfully on 2026-08-01; host/joiner feel comparison remains required. |
| Stable keyboard movement with independent mouse aim | **Implemented but unverified** | WASD/arrow axes are separated from the actor-relative left-stick axes and move against the local top-down camera yaw. Turning the wizard with mouse/Q/E no longer rotates the keyboard movement frame. Existing movement impairment and authority paths are unchanged. `WizardStaffEditor` built successfully on 2026-08-04; human feel validation remains required. |
| Joiner movement/facing correction alignment | **Implemented but unverified** | Autonomous-client facing now travels through CharacterMovement's control-rotation data instead of a competing per-input yaw RPC, while the server retains bounded turn validation. Client movement modifiers use replicated Slosh so prediction matches server speed, acceleration, braking, and turn penalties. Respawns synchronize actor and control yaw. `WizardStaffEditor` built successfully on 2026-08-01; a two-machine movement/facing comparison remains required. |

## Cauldron Catastrophe

| Area | Status | Evidence |
| --- | --- | --- |
| Cauldron trial classes and tuning | **Implemented and verified** | Arena, vial, hazard, ingredient, and deposit-arc classes are in current source and the focused Trial behavior has been human-tested. |
| Vial pickup, stack effects, banking/deposit, deposit-triggered hazards, curse, scoring/readability | **Implemented and verified** | Human testing confirmed deposit-triggered hazard mapping, long-staff intake reliability, Slosh-scaled slippery behavior, and the eight-bomb sector-spread cursed-orb bombardment alongside the existing vial/banking loop. Detailed behavior is in [CauldronCatastropheDesign.md](CauldronCatastropheDesign.md). |
| Online vial-segment identity readability | **Implemented but unverified** | GameMode maps its authoritative tagged vial stack onto a compact display-only `None`/`Speed`/`BurdeningPower` array on each wizard. Remote clients recolor the corresponding staff meshes without receiving segment tags or gameplay authority. Grant, snap, bank, spill, and cleanup share the existing vial refresh boundaries. `WizardStaffEditor` built successfully on 2026-08-09; a two-player mixed-vial ordering and cleanup check remains required. |
| Normal match rotation | **Implemented and verified locally and in listen-server PIE** | Complete human playthroughs confirmed Party Hall between every Trial, Grand Wizard Final startup, and return to intermission. |
| Arena separation and start placement | **Implemented and verified locally** | Human playthroughs confirmed the separated runtime arenas and corrected transition placement. |
| Results/intermission gameplay cleanup | **Implemented and verified** | Results immediately neutralizes Cauldron hazards, movement effects, curse/banking state, bombs, arcs, vials, and callbacks while retaining only the inert transition floor. |

## Online and Steam

| Area | Status | Evidence |
| --- | --- | --- |
| Private-playtest main menu | **Partially implemented** | `WizardStaff_MainMenu` starts Local, Steam host, Steam join, Controls, and Quit. BuildID `24510908` completed a two-account Steam connection. Current source adds a local-only gameplay Escape submenu with Resume, Controls, and explicit Return to Main Menu; it preserves the existing session teardown and does not pause server gameplay. `WizardStaffEditor` built successfully on 2026-08-01; Local and two-player validation remain required. |
| Online Party Hall host-ready gate | **Implemented but unverified** | Steam Build `24393938` verified that a host-only Party Hall keeps its Trial timer frozen. Build `24394181` accepted a host Ready Bell bonk and started the countdown, but the session contained an unintended secondary local player left over from the preceding Local test. Inactive Build `24504173` contains the cleanup that removes extra local players before Steam host/join setup; the corrected host-only and real-client Ready Bell paths remain unverified. |
| Two-client PIE / direct-connect baseline | **Implemented and verified** | Human-observed separate-process direct-connect full loop plus a 2026-07-15 two-player listen-server PIE full-loop pass are recorded. |
| Prototype-arena phase visibility replication | **Implemented and verified** | Both listen-server PIE windows completed the full loop with correct arena handoffs after the actor-owned replicated phase-presentation contract was added. |
| Dirty-checked GameState/PlayerState mirrors | **Implemented and verified** | Unchanged PlayerState mirrors no longer force network updates, unchanged GameState fields are not rewritten, display timers update at 0.1-second granularity, progress mirrors at 1%, and empty event-feed clears no longer produce redundant sequences. The editor build and subsequent human validation both passed on 2026-07-15. |
| Stable PlayerState slot-first identity lookup | **Implemented and verified** | `WizardDisplaySlot` is preserved once assigned and is now the primary lookup for wizard, controller/spawn, replicated PlayerState, scoring, Favor, HUD identity, and Arcane Pinball readability paths. New PlayerStates receive the first unused nonnegative slot; `PlayerId` and controller order remain pre-assignment compatibility fallbacks. `WizardStaffEditor` built successfully and a subsequent two-player listen-server PIE session passed on 2026-07-15. Reconnect identity restoration remains deferred. |
| Steam host session creation | **Implemented and verified** | Prior host log and smoke records report Steam subsystem/session creation and listen travel. |
| Steam find/join/travel on two machines/two accounts | **Implemented and verified for initial connection** | BuildID `24510908` successfully connected a host and remote friend through the in-game Steam flow on 2026-08-01. The short session exposed confirmed rejoin/session-lifecycle, client presentation, movement smoothness, sensitivity, and intermission ring-out gaps. Steam friends-list invite/join handling remains intentionally deferred. |
| Steam build compatibility gating | **Implemented but unverified** | `WIZARDSTAFF_BUILD` derives from Unreal's local network checksum plus explicit `ProjectVersion=0.1.0-private.2`. Build `24636896` exposed that Steam can translate the lobby result's numeric `BuildUniqueId` even when both machines report the exact same versioned metadata string, causing a false rejection. Search now requires the project map and exact readable build identity; the numeric value is diagnostic only, while Unreal's connection handshake remains the final compatibility guard. A replacement packaged matching/mismatched build check remains required; future network-incompatible releases must bump `ProjectVersion`. |
| Steam departure/rejoin lifecycle contract | **Implemented but unverified** | Before a new lobby search, the join path destroys any stale local `GameSession`. The host advertises/accepts joins only while waiting in Party Hall; a remote departure aborts the interrupted match, bumps to a clean match generation, clears gameplay state, returns the host to Party Hall, and reopens the session. A disconnected client returns to the menu with a readable status. `WizardStaffEditor` built successfully on 2026-08-09; a two-account active-Trial disconnect/rejoin test remains required. This does not restore the departed identity or score. |
| Steam private build installation | **Implemented and verified** | A private Steam build was installed and a full playthrough was reported working. |
| Current `private_test` playtest build | **Implemented and verified** | BuildID `24238419` (depot manifest `593837560765890906`) packaged, previewed, uploaded, and was assigned only to `private_test` on 2026-07-16. A Steam-installed human playthrough verified the complete current loop and in-game Party Hall standings board. `default` was intentionally left unchanged. |
| Current `private_test` Steam menu candidate | **Partially implemented** | BuildID `24343969` (depot manifest `4898219811341155544`) is assigned only to `private_test`. A Steam-launched check verified no-session Join feedback and Steam host travel into a waiting Party Hall. It lacks the subsequently fixed Escape return route. `default` remains unchanged. |
| Current `private_test` menu input / Party Hall readiness candidate | **Partially implemented** | BuildID `24393938` was assigned to `private_test` and human-tested on 2026-07-25. Local gameplay and Steam host travel worked, and the host-only Party Hall timer remained frozen. Escape failed to return either Local or Online play to the menu. Current source contains a replacement pawn-input route that builds but has not yet been packaged. |
| Latest inactive Local-to-Online cleanup candidate | **Implemented but unverified** | BuildID `24504173` (depot manifest `8539026792454398946`) packaged and uploaded successfully on 2026-07-31 with no branch assignment. It removes secondary local players before Steam host/join setup. Assign only to `private_test`, then test Play Local -> Escape -> Host Online; the host must have only P1 and the Ready Bell must remain blocked until a real P2 joins. |
| Current Steam join/timeout private-test candidate | **Partially verified** | BuildID `24510908` (depot manifest `6957954008743286605`) connected two machines/two accounts through the in-game Host/Join flow on 2026-08-01. Initial connection is verified; timeout messaging was not explicitly observed, and reconnecting after leaving failed even after another session was hosted. Keep `default` unchanged while follow-up issues are addressed. |
| Latest inactive stable-WASD candidate | **Implemented but unverified** | BuildID `24561669` (depot manifest `4223478960010625334`) was packaged and uploaded successfully on 2026-08-04 with no branch assignment. It includes the prior multiplayer cleanup plus stable screen-relative WASD/arrow movement with independent mouse/Q/E staff aim. Assign only to `private_test`; Local feel and two-account Steam validation remain required. |
| Superseded Steam Escape candidates | **Failed / not for promotion** | BuildID `24346104` (depot manifest `4016253160872424587`) was assigned only to `private_test` but did not return the host from Party Hall. Inactive Build `24346698` (depot manifest `7182046382881412670`) replaced the route with a direct gameplay-controller key binding, but that experiment regressed Local PIE keyboard input and was removed from source on 2026-07-24. Neither build should be promoted. |
| Runtime fallback lighting for packaged/private Steam build | **Implemented and verified** | Lighting bug was reported, fixed through a follow-up build, and confirmed by a later full playthrough. |
| Steam leaderboard/stat submission | **Implemented but unverified** | GameMode computes each human result and delivers it through that player's owner-only reliable PlayerState RPC. The authenticated Steam client can queue the write only through a private GameInstance seam that verifies the delivering PlayerState is its exact local owner, is not a bot, and has the same display slot. Duplicate match generations remain guarded. `WizardStaffEditor` built successfully on 2026-08-09; one-write-per-owner, flush, KeepBest, and Steamworks read-back still require a two-account private-build test. This client-mediated Steam API scaffold is not backend anti-cheat validation. |
| Production lobby/UI/matchmaking/invites/reconnect | **Planned / deferred** | Explicitly outside current scope. |

Focused unverified behavior is tracked once in [KNOWN_ISSUES.md](KNOWN_ISSUES.md); repeatable checks live in [PLAYTEST_PLAN.md](PLAYTEST_PLAN.md).

## Deferred by Intent

- Production lobby/browser/matchmaking/friend invite UX.
- Robust reconnect, host migration, dedicated servers, and cross-internet/NAT validation.
- Client prediction, rewind, lag compensation, exact cross-client physics fidelity.
- Replicated loose snapped-segment physics.
- Production UI/UMG conversion, cosmetics, progression, shops, inventory, chat, voice, analytics.

See [ROADMAP.md](ROADMAP.md) for suggested next validation work and [KNOWN_ISSUES.md](KNOWN_ISSUES.md) for risks.
